-# %p#notice= notice
%h1 Posts
%table
  %thead
    %tr
      %th{:colspan => "3"}
  %tbody
    - @posts.each do |post|
      %tr
        %td= post.title
        %td= link_to 'Show', post
        %td= link_to 'Edit', edit_post_path(post)
        %td= link_to 'Destroy', post, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }
%br/
= link_to 'New Post', new_post_path
